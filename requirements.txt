# Зависимости для высокопроизводительного сигнального сервера Sferium Homes (FastAPI + WebSocket)
# Оптимизировано для Python 3.12+ на серверах Ubuntu Linux (Германия)

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
firebase-admin>=6.5.0
pydantic>=2.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
uvloop>=0.19.0; sys_platform != 'win32'
